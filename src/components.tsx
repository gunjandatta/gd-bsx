import { Components } from "gd-bs";
import { Base } from "./base";

// Accordion
export class Accordion extends Base<Components.IAccordionProps> {
    // Render the bootstrap component
    onRender(props) { Components.Accordion(props); }
}

// Alert
export class Alert extends Base<Components.IAlertProps> {
    // Render the bootstrap component
    onRender(props) { Components.Alert(props); }
}

// Badge
export class Badge extends Base<Components.IBadgeProps> {
    // Render the bootstrap component
    onRender(props) { Components.Badge(props); }
}

// Button
export class Button extends Base<Components.IButtonProps> {
    // Render the bootstrap component
    onRender(props) { Components.Button(props); }
}

// ButtonGroup
export class ButtonGroup extends Base<Components.IButtonGroupProps> {
    // Render the bootstrap component
    onRender(props) { Components.ButtonGroup(props); }
}

// Card
export class Card extends Base<Components.ICardProps> {
    // Render the bootstrap component
    onRender(props) { Components.Card(props); }
}

// CardGroup
export class CardGroup extends Base<Components.ICardGroupProps> {
    // Render the bootstrap component
    onRender(props) { Components.CardGroup(props); }
}

// Carousel
export class Carousel extends Base<Components.ICarouselProps> {
    // Render the bootstrap component
    onRender(props) { Components.Carousel(props); }
}

// Checkbox Group
export class CheckboxGroup extends Base<Components.ICheckboxGroupProps> {
    // Render the bootstrap component
    onRender(props) { Components.CheckboxGroup(props); }
}

// Collapse
export class Collapse extends Base<Components.ICollapseProps> {
    // Render the bootstrap component
    onRender(props) { Components.Collapse(props); }
}

// Dropdown
export class Dropdown extends Base<Components.IDropdownProps> {
    // Render the bootstrap component
    onRender(props) { Components.Dropdown(props); }
}

// Form
export class Form extends Base<Components.IFormProps> {
    // Render the bootstrap component
    onRender(props) { Components.Form(props); }
}

// Form Control
export class FormControl extends Base<Components.IFormControlProps> {
    // Render the bootstrap component
    onRender(props) { Components.FormControl(props); }
}

// Input Group
export class InputGroup extends Base<Components.IInputGroupProps> {
    // Render the bootstrap component
    onRender(props) { Components.InputGroup(props); }
}

// Jumbotron
export class Jumbotron extends Base<Components.IJumbotronProps> {
    // Render the bootstrap component
    onRender(props) { Components.Jumbotron(props); }
}